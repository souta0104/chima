{
  "name": "chima",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests && bash tests/install.test.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
